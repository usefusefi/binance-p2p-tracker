# Environment
ENVIRONMENT=development  # Set to 'production' for prod mode

# Server Configuration
PORT=8080
HOST=127.0.0.1
DEBUG=True
FLASK_ENV=development

# API Configuration
DEV_API_URL=http://127.0.0.1:8080/api/p2p-data
PROD_API_URL=https://your-azure-app.azurewebsites.net/api/p2p-data
API_BASE_URL=${DEV_API_URL}  # Will be overridden when ENVIRONMENT=production

# Default Values
DEFAULT_TRADE_TYPE=BUY
DEFAULT_FIAT=USD
DEFAULT_CRYPTO=USDT

# CORS Configuration
CORS_ORIGINS=*

# Static Files
STATIC_FOLDER=static
TEMPLATE_FOLDER=. 